spring:
  application:
    name: jira-mini
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}
  secret_refresh_token: ${JWT_SECRET_REFRESH_TOKEN}
  expiration-refresh-token: ${JWT_EXPIRATION_REFRESH_TOKEN}