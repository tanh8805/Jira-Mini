spring:
  application:
    name: jira-mini
  datasource:
    url: jdbc:postgresql://postgresql:5432/jira-db
    username: admin
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false